require "spec_helper"

RSpec.describe FigaroSekrets do
  it "has a version number" do
    expect(FigaroSekrets::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
